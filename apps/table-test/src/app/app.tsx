import { Table } from '@table-test/tablers';
import React from 'react';
import './app.less';


export const App = () => {
  /*
   * Replace the elements below with your own.
   *
   * Note: The corresponding styles are in the ./app.less file.
   */
  return (
    <Table columns={[]} data={[]}/>
  );
};

export default App;
