import React from 'react';

import './tablers.less';

/* eslint-disable-next-line */
export interface TablersProps {}

export const Tablers = (props: TablersProps) => {
  return (
    <div>
      <h1>Welcome to tablers!</h1>
    </div>
  );
};

export default Tablers;
